package unity.controller.cron;

import org.slim3.controller.Controller;
import org.slim3.controller.Navigation;

public class RelationTagController extends Controller {

    @Override
    protected Navigation run() throws Exception {

        // List<GameData> gameData = Datastore.query(GameData.class).asList();

        // List<TagGame> tagGame = Datastore.query(TagGame.class).asList();

        return null;
    }

}
